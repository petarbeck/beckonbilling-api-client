openapi: 3.1.0
info:
  title: Beckon Billing API
  version: "1.0.0"
  description: |
    Public REST API (v1) for Beckon Billing.

    This document is the **canonical contract** for the API. It is consumed by
    the official PHP client (`beckonbilling/api-client`) and can be imported
    directly into Postman, Insomnia, or an OpenAPI code generator.

    ## Authentication

    Every request carries an API token as a bearer token:

    ```
    Authorization: Bearer bbp_<your-token>
    ```

    Two token flavours authorise through the same per-feature access model:

    - **Organisation tokens** (`bbp_...`) - created in the portal under
      *Firma -> API-Zugriff*, bound to one organisation, carry an explicit
      `{feature: none|view|full}` grant map plus the `send`/`bank` capabilities.
    - **User tokens** - minted by `POST /auth/login`; act as the signed-in user
      across all their organisations (owner = Full on every feature). Select an
      organisation per request with `?organisation=<uuid>` (optional when the
      user has exactly one).

    ## Permissions & capabilities

    Each entity maps to one member feature. Read/PDF actions need the feature at
    **View**; every mutation needs **Full**. On top of that, two boolean
    capabilities gate third-party effects, default **OFF** for everyone except
    the organisation owner:

    - `send` - emailing a customer (quote/invoice issue + send).
    - `bank` - moving money / marking paid (invoice `set-paid`).

    A missing capability returns 403 with `error.key` `send_not_permitted` /
    `bank_not_permitted`.

    Two collections are **read-only** here - `units` and `document-templates`.
    A caller must be able to READ them to build a valid document; managing them
    stays a portal action, and every method but `GET` answers 405. They are
    gated differently, because only one of them says anything about the
    organisation: **`units` needs no permission at all** (it is the same fixed
    system list for everybody), while `document-templates` needs View on
    `quotes` or `outbound_invoices`, and its rows are filtered to the kinds
    that token may actually view.


    **`/document-terms` was REMOVED** and now answers 404;
    `/document-templates` replaced it, and the input key `document_term_id`
    answers 422 `document_term_id_retired` in favour of `document_template_id`.

    ## Conventions

    - **IDs** are opaque UUID strings; integer ids are never exposed. (One
      exception, flagged where it occurs: `RecurringInvoice.document_ids`.)
    - **Lists** are paginated (`limit` default 100, max 500) and return
      `{ data, total, limit, offset }`.
    - **Creating answers 201**; reading, updating and every action answer 200.
      Deleting answers 200 with `{ "deleted": true }`.
    - **Updates are partial.** A `PUT` applies the keys it recognises and leaves
      the rest of the record alone; a key it does not read is silently ignored
      (see strict mode below).
    - **Dates** are ISO-8601: calendar-day fields (`issue_date`, `due_date`,
      `valid_until`, `first_run_date`) are `YYYY-MM-DD`; timestamps are full
      offset datetimes. Unset dates are `null`.
    - **Amounts** are decimal net numbers; `tax_percent` is a percentage.
    - **Errors** have the shape `{ "error": { code, message, key } }`; branch on
      the stable `key`.

    ## Strict mode (`?strict=1`)

    Append `?strict=1` to any `POST`/`PUT` and an unrecognised body key answers
    **400 `unrecognised_keys`**, naming the offenders, instead of being silently
    discarded. Opt-in, so no existing client can break, and worth turning on in
    development: the default behaviour cannot tell "saved" from "dropped", which
    is how a renamed field can cost weeks of silent data loss.

    Two caveats, both real:

    - It checks the request's SHAPE only, and runs BEFORE authentication - so it
      also works on `POST /auth/login`, which has no token yet.
    - It **silently does nothing on `/articles` and `/article-categories`**.
      Those two endpoints declare no key list, and a global flag must not turn
      an undeclared endpoint into a hard failure. Every other entity here is
      covered, so do not read a pass on those two as a validated request.

    ## Units are a fixed system catalogue, and an article and a position use it differently

    `GET /units` is a **fixed list of 31 system units**, identical for every
    organisation. It is no longer a vocabulary each organisation keeps and
    grows: nothing you send can add to it, nothing can be renamed out of it,
    and there is no `unit_too_long` refusal any more. Each entry has a
    lowercase `key` (`piece`, `hour`, `month`) and, per language, the printed
    short form, the long label and the plural.

    The two places a unit appears do **not** take the same value.

    - **An article's `unit` is a KEY**, and an unknown one answers
      **422 `unit_unknown`**. It is matched exactly, lowercase, with no
      case-folding. A printed short form is not a key, so `Stk.`, `h` and `pc.`
      are all refused; send `piece`, `hour`, `month`. Omitting `unit` on create
      stores `piece`; an explicit `""` stores "no unit" and is kept.
    - **A position's `unit` is never refused.** Send a key and the server
      resolves it to the printed short form and plural **in the issuing
      organisation's language**, then snapshots both onto the line: `piece`
      reads back as `pc.`/`pcs.` for an English organisation and `Stk.`/`""`
      for a German one. Send anything else and it is taken verbatim as printed
      text. Positions therefore store TEXT, never a key, which is what keeps an
      issued document printing what it printed the day it was issued.

    Because a position's `unit` is resolved on the way in, **the value you read
    back is not always the value you sent**. That is normal here, and sending
    the resolved value straight back is stable.
  license:
    name: MIT
    url: https://github.com/petarbeck/beckonbilling-api-client/blob/main/LICENSE
  contact:
    name: Beckon Billing
    url: https://www.beckonbilling.com

servers:
  - url: https://{host}/api/v1
    variables:
      host:
        default: portal.beckonbilling.com
        description: Your Beckon Billing portal host.

security:
  - bearerAuth: []

tags:
  - name: Auth
    description: User-token authentication (no organisation scope).
  - name: Customers
  - name: Article categories
  - name: Articles
  - name: Units
    description: The fixed system catalogue of units. Read-only, same for every organisation.
  - name: Document templates
    description: Document templates ("Vorlagen"). Read-only. Replaced "Document terms".
  - name: Quotes
  - name: Outbound invoices
  - name: Recurring invoices

paths:
  /auth/register:
    post:
      tags: [Auth]
      summary: Self-registration (pending account + activation email)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, name]
              properties:
                email: { type: string, format: email }
                name: { type: string }
                language: { type: string, example: de }
      responses:
        "200":
          description: Registration accepted.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "422": { $ref: "#/components/responses/Error" }

  /auth/login:
    post:
      tags: [Auth]
      summary: Sign in and mint a user token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string }
                tfa_code: { type: string, description: TOTP, recovery, or emailed fallback code. }
                remember_device: { type: boolean }
                device_token: { type: string, description: A previously issued 30-day trusted-device token. }
      responses:
        "200":
          description: Authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  token: { type: string }
                  expires_at: { type: string, format: date-time }
                  device_token: { type: string, nullable: true }
                  user: { type: object, additionalProperties: true }
                  organisations:
                    type: array
                    items: { type: object, additionalProperties: true }
        "401":
          description: Bad credentials, or `error.key = tfa_required` until a `tfa_code` is supplied.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /auth/me:
    get:
      tags: [Auth]
      summary: Current user + accessible organisations (session restore)
      responses:
        "200":
          description: The current user.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "401": { $ref: "#/components/responses/Error" }

  /customers:
    get:
      tags: [Customers]
      summary: List customers
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of customers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerList" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
    post:
      tags: [Customers]
      summary: Create a customer
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerInput" }
      responses:
        "201":
          description: The created customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Customer" }
        "422": { $ref: "#/components/responses/Error" }

  /customers/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Customers]
      summary: Read one customer
      responses:
        "200":
          description: The customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Customer" }
        "404": { $ref: "#/components/responses/Error" }
    put:
      tags: [Customers]
      summary: Update a customer (partial)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerInput" }
      responses:
        "200":
          description: The updated customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Customer" }
    delete:
      tags: [Customers]
      summary: Soft-delete a customer
      responses:
        "200": { description: Deleted. }

  /article-categories:
    get:
      tags: [Article categories]
      summary: List article categories
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of categories.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleCategoryList" }
    post:
      tags: [Article categories]
      summary: Create a category
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: >-
                NOTE: `?strict=1` does NOT validate this endpoint's body - it
                declares no key list, so an unrecognised key is silently
                discarded here even with the flag on.
              properties:
                label: { type: string }
                fibu_code: { type: string }
      responses:
        "201":
          description: The created category.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleCategory" }

  /article-categories/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Article categories]
      summary: Read one category
      responses:
        "200":
          description: The category.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleCategory" }
    put:
      tags: [Article categories]
      summary: Update a category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: >-
                NOTE: `?strict=1` does NOT validate this endpoint's body - it
                declares no key list, so an unrecognised key is silently
                discarded here even with the flag on.
              properties:
                label: { type: string }
                fibu_code: { type: string }
      responses:
        "200":
          description: The updated category.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleCategory" }
    delete:
      tags: [Article categories]
      summary: Soft-delete a category
      responses:
        "200": { description: Deleted. }

  /articles:
    get:
      tags: [Articles]
      summary: List articles
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of articles.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleList" }
    post:
      tags: [Articles]
      summary: Create an article
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ArticleInput" }
      responses:
        "201":
          description: The created article.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Article" }

  /articles/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Articles]
      summary: Read one article
      responses:
        "200":
          description: The article.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Article" }
    put:
      tags: [Articles]
      summary: Update an article (partial)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ArticleInput" }
      responses:
        "200":
          description: The updated article.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Article" }
    delete:
      tags: [Articles]
      summary: Soft-delete an article
      responses:
        "200": { description: Deleted. }

  /articles/{id}/variants:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Articles]
      summary: List an article's variants
      description: >-
        The variants of one article. A variant only has meaning together with
        its article, which is why it is a sub-collection rather than an entity
        of its own. Needs the `articles` feature at view.
      parameters:
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of variants.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleVariantList" }
    post:
      tags: [Articles]
      summary: Create a variant of this article
      description: Needs the `articles` feature at full.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ArticleVariantInput" }
      responses:
        "201":
          description: The created variant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleVariant" }

  /articles/{id}/variants/{variantId}:
    parameters:
      - $ref: "#/components/parameters/id"
      - name: variantId
        in: path
        required: true
        schema: { type: string }
        description: The variant uuid. It must belong to the article in the path.
      - $ref: "#/components/parameters/organisation"
    put:
      tags: [Articles]
      summary: Update a variant (partial)
      description: >-
        Needs the `articles` feature at full. Pass `null` for a field to clear
        the override and inherit the article's value again. There is
        deliberately no DELETE: a variant a document already used is retired in
        the portal, and the document keeps the values and the label it
        snapshotted either way.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ArticleVariantInput" }
      responses:
        "200":
          description: The updated variant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ArticleVariant" }

  /units:
    get:
      tags: [Units]
      summary: List the system unit catalogue
      description: >-
        The fixed list of 31 system units, the same for every organisation.
        Each row carries the `key` an ARTICLE's `unit` must be one of, and the
        printed forms per language a POSITION's `unit` resolves to. Read it to
        learn the keys: an article's `unit` is validated against them and an
        unknown one answers 422 `unit_unknown`.


        **No permission is required.** The list is the same for everyone and
        discloses nothing about the organisation, so any valid token may read
        it. It used to be gated on holding View for one of the document
        features, back when it was the organisation's own vocabulary.
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of units.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UnitList" }
        "403": { $ref: "#/components/responses/Error" }
        "405":
          description: Read-only - every method but GET is refused.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /units/{id}:
    get:
      tags: [Units]
      summary: Read one unit
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: The unit.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unit" }
        "404": { $ref: "#/components/responses/Error" }

  /document-templates:
    get:
      tags: [Document templates]
      summary: List document templates ("Vorlagen")
      description: >-
        Where the ids for the write-only `document_template_id` input come from,
        together with the two terms texts and the day count each one would apply.


        This replaced `/document-terms`, which was removed. A template is the
        bigger thing a terms preset used to be a part of: besides the wording and
        the days it also carries the printed footer, the cover-mail text, the
        attachments a new document starts with, and - per kind - the quote's
        default down payment or the invoice's bank details. **The organisation's
        default template IS its document setting**, which is why those fields
        moved off the organisation and onto this record.


        Readable with View on `quotes` or `outbound_invoices`, and the ROWS are
        filtered to the kinds this token may actually view - a quotes-only token
        never receives the invoice templates. Asking for a `kind` it cannot view
        answers an EMPTY LIST rather than 403, which would leak that the other
        kind exists.
      parameters:
        - $ref: "#/components/parameters/organisation"
        - name: kind
          in: query
          required: false
          description: >-
            Restrict to one document type. Note the value is `invoice`, NOT
            `outbound_invoice` - the kind `/document-terms` used.
          schema: { type: string, enum: [quote, invoice] }
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of templates.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DocumentTemplateList" }
        "403": { $ref: "#/components/responses/Error" }
        "405":
          description: Read-only - every method but GET is refused.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /document-templates/{id}:
    get:
      tags: [Document templates]
      summary: Read one document template
      description: >-
        A template of a kind this token may not view answers the same 404
        `document_template_not_found` as an unknown or foreign id.
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: The template.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DocumentTemplate" }
        "404": { $ref: "#/components/responses/Error" }

  /quotes:
    get:
      tags: [Quotes]
      summary: List quotes
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of quotes.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuoteList" }
    post:
      tags: [Quotes]
      summary: Create a draft quote
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuoteInput" }
      responses:
        "201":
          description: The created draft.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Quote" }

  /quotes/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Quotes]
      summary: Read one quote (incl. positions)
      responses:
        "200":
          description: The quote.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Quote" }
    put:
      tags: [Quotes]
      summary: Update a quote
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuoteInput" }
      responses:
        "200":
          description: The updated quote.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Quote" }
    delete:
      tags: [Quotes]
      summary: Soft-delete a quote
      responses:
        "200": { description: Deleted. }

  /quotes/{id}/issue:
    post:
      tags: [Quotes]
      summary: Issue a draft (assigns the number)
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: >-
            The issued quote, plus `detached_positions` when any line lost a
            retired catalog reference on the way through.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Quote"
                  - type: object
                    properties:
                      detached_positions: { $ref: "#/components/schemas/DetachedPositions" }

  /quotes/{id}/send:
    post:
      tags: [Quotes]
      summary: Email the quote (requires the `send` capability)
      description: >-
        Issues drafts first and resets a won/lost quote back to issued.


        **The body is an envelope, not a bare quote** - unlike every other quote
        action, this answers `{ sent_to, quote }`. A quote with no recipient
        address and no customer to fall back on is refused with 422
        `recipient_email_missing`; a mail failure is 502 `mail_send_failed`
        (unlike an invoice issue, which sends best-effort and reports
        `send_error` on a 200).
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                document_ids:
                  type: array
                  items: { type: string }
                  description: Override the attached documents for this send (empty = attach none).
      responses:
        "200":
          description: Where it went, and the quote as it now stands.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sent_to: { type: string, description: The address it was mailed to. }
                  quote: { $ref: "#/components/schemas/Quote" }
                  detached_positions: { $ref: "#/components/schemas/DetachedPositions" }
        "403":
          description: Missing `send` capability (`error.key = send_not_permitted`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "422":
          description: No recipient address (`error.key = recipient_email_missing`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "502":
          description: The mail could not be sent (`error.key = mail_send_failed`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /quotes/{id}/convert:
    post:
      tags: [Quotes]
      summary: Create a draft invoice from the quote
      description: >-
        Requires `quotes` Full and `outbound_invoices` Full.

        A quote whose lines are ALL `billing_mode: recurring` has no one-time
        invoice to create and is refused with 409 `quote_is_recurring_only` - it
        converts to a recurring invoice, which this v1 action does not create.
        Use the portal for a mixed or recurring-only quote.


        **`scope` decides WHICH invoice this creates** - the whole thing, a down
        payment, or the remainder. Omitting it keeps the behaviour this action
        has always had. See `OutboundInvoice.invoice_scope` for how a down
        payment and a final invoice add up.


        **The new invoice is seeded from a TEMPLATE, not from the quote.** Its
        `terms_text`, `payment_terms_text` and `due_date` come from the
        customer's own settings, else the customer's template, else the
        organisation's default invoice template - the same chain a hand-made
        invoice resolves. The quote's own terms never travel: they say different
        things on the two documents, and the quote wording holds
        `{valid_until}`, which an invoice cannot resolve. Note `due_date` is
        therefore a date on a freshly converted invoice; it used to be `null`.


        **Its `document_ids` do NOT follow that chain.** Attachments come only
        from the ORGANISATION's default invoice template; a customer's own
        template never contributes them, here or on a hand-made invoice. A
        customer whose template carries no attachments therefore still gets the
        organisation default's.


        Refusals worth handling, all by `error.key`: `invoice_scope_unknown`
        (422, a `scope` outside the three values); `customer_required` (409);
        `quote_has_no_positions` (409); `quote_is_recurring_only` (409);
        `quote_already_invoiced` (409); and, for the two staged scopes,
        `quote_has_no_deposit` (422, `scope=deposit` on a quote carrying none),
        `quote_deposit_base_empty` (422), `deposit_invoice_exists` (409),
        `quote_has_deposit_invoice` (409, `scope=full` once a down payment
        exists - the rest belongs on a final invoice), `no_deposit_invoice`
        (409, `scope=final` with no ISSUED down payment to deduct),
        `quote_has_draft_invoice` (409, issue or delete it first) and
        `final_invoice_exists` (409).
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                scope:
                  type: string
                  enum: [full, deposit, final]
                  default: full
                  description: >-
                    Which invoice to create. `full` bills the whole one-time
                    part, as this action always did - omitting `scope`, or
                    sending null or an empty string, means exactly that.
                    `deposit` bills only the quote's down payment; `final` bills
                    the remainder, deducting an already-issued down payment.
                    Anything else is 422 `invoice_scope_unknown` - it is refused
                    rather than coerced, because falling back to `full` would
                    silently bill the entire amount.
      responses:
        "201":
          description: The new invoice id + the (converted) quote.
          content:
            application/json:
              schema:
                type: object
                properties:
                  outbound_invoice_id: { type: string }
                  quote: { $ref: "#/components/schemas/Quote" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }

  /quotes/{id}/pdf:
    get:
      tags: [Quotes]
      summary: Download the final PDF (409 for drafts)
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: The PDF.
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "409": { $ref: "#/components/responses/Error" }

  /outbound-invoices:
    get:
      tags: [Outbound invoices]
      summary: List invoices
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
        - { name: status, in: query, schema: { type: string, enum: [draft, issued] } }
        - { name: paid, in: query, schema: { type: integer, enum: [0, 1] } }
        - { name: sent, in: query, schema: { type: integer, enum: [0, 1] } }
        - { name: cancelled, in: query, schema: { type: integer, enum: [0, 1] } }
        - { name: customer_id, in: query, schema: { type: string } }
        - { name: q, in: query, schema: { type: string }, description: Free-text search. }
      responses:
        "200":
          description: A page of invoices.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoiceList" }
    post:
      tags: [Outbound invoices]
      summary: Create a draft invoice
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OutboundInvoiceInput" }
      responses:
        "201":
          description: The created draft.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }

  /outbound-invoices/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Outbound invoices]
      summary: Read one invoice (incl. positions)
      responses:
        "200":
          description: The invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }
    put:
      tags: [Outbound invoices]
      summary: Update a draft (full) or an issued invoice (limited fields)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OutboundInvoiceInput" }
      responses:
        "200":
          description: The updated invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }
    delete:
      tags: [Outbound invoices]
      summary: Delete a draft (409 for issued - cancel instead)
      responses:
        "200": { description: Deleted. }
        "409": { $ref: "#/components/responses/Error" }

  /outbound-invoices/{id}/issue:
    post:
      tags: [Outbound invoices]
      summary: Issue the draft and auto-send it (requires the `send` capability)
      description: >-
        Assigns the number, exports the PDF, mints the online payment link and
        mails it to the customer.


        **Three of those are best-effort and never fail the issue, so read the
        result rather than the status code.** `send_error` means the mail did
        not go out. `payment_link_error` means the invoice offers online payment
        with no Stripe link behind it - the customer has no way to pay the way
        the document tells them to. `detached_positions` means a line lost a
        retired catalog reference; no figure changed, but it is reported rather
        than done silently. Each key is present ONLY when that thing happened.
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                document_ids: { type: array, items: { type: string } }
      responses:
        "200":
          description: The issued invoice, plus any side effect that did not succeed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/OutboundInvoice"
                  - type: object
                    properties:
                      send_error:
                        type: string
                        description: Present only when mailing the customer failed. The invoice IS issued.
                      payment_link_error:
                        type: string
                        description: >-
                          Present only when minting the Stripe payment link failed. The invoice IS
                          issued, but its online payment option has no link behind it.
                      auto_matched:
                        type: integer
                        description: >-
                          How many existing bank transactions were matched to this invoice as it was
                          issued (the customer had already paid). Present only when at least one was.
                      detached_positions: { $ref: "#/components/schemas/DetachedPositions" }
        "403": { $ref: "#/components/responses/Error" }
        "409":
          description: Not a draft, or it has no positions.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /outbound-invoices/{id}/send:
    post:
      tags: [Outbound invoices]
      summary: (Re-)send an issued invoice (requires the `send` capability)
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                document_ids: { type: array, items: { type: string } }
      responses:
        "200":
          description: The sent invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }
        "403": { $ref: "#/components/responses/Error" }

  /outbound-invoices/{id}/cancel:
    post:
      tags: [Outbound invoices]
      summary: Cancel an issued invoice (creates the credit note)
      description: >-
        Answers the **credit note that was created**, not the invoice that was
        cancelled - read `cancels_outbound_invoice_id` on it to name the other
        half.


        Cancelling an invoice that never received money settles that credit note
        automatically: the pair nets to zero and nobody will ever pay it.
        Cancelling one that was paid even in part deliberately leaves it OPEN,
        because that is a real refund owed to the customer.
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: The newly created credit note.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }
        "409":
          description: A draft (delete it instead), or already cancelled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /outbound-invoices/{id}/set-paid:
    put:
      tags: [Outbound invoices]
      summary: Record / remove an untracked payment (requires the `bank` capability)
      description: >
        `true` records an untracked payment over the open amount; `false`
        removes the paid mark and is refused (409) while transaction-linked
        payments exist.
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [paid]
              properties:
                paid: { type: boolean }
      responses:
        "200":
          description: The invoice with recomputed paid state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OutboundInvoice" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /outbound-invoices/{id}/pdf:
    get:
      tags: [Outbound invoices]
      summary: Download the final PDF (409 for drafts)
      parameters:
        - $ref: "#/components/parameters/id"
        - $ref: "#/components/parameters/organisation"
      responses:
        "200":
          description: The PDF.
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "409": { $ref: "#/components/responses/Error" }

  /recurring-invoices:
    get:
      tags: [Recurring invoices]
      summary: List recurring invoices
      parameters:
        - $ref: "#/components/parameters/organisation"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/offset"
      responses:
        "200":
          description: A page of recurring invoices.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RecurringInvoiceList" }
    post:
      tags: [Recurring invoices]
      summary: Create a recurring invoice
      parameters: [ { $ref: "#/components/parameters/organisation" } ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecurringInvoiceInput" }
      responses:
        "201":
          description: The created recurring invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RecurringInvoice" }

  /recurring-invoices/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/organisation"
    get:
      tags: [Recurring invoices]
      summary: Read one recurring invoice
      responses:
        "200":
          description: The recurring invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RecurringInvoice" }
    put:
      tags: [Recurring invoices]
      summary: Update a recurring invoice (partial)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecurringInvoiceInput" }
      responses:
        "200":
          description: The updated recurring invoice.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RecurringInvoice" }
    delete:
      tags: [Recurring invoices]
      summary: Soft-delete a recurring invoice
      responses:
        "200": { description: Deleted. }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: An organisation token (`bbp_...`) or a user token.

  parameters:
    organisation:
      name: organisation
      in: query
      required: false
      description: Organisation UUID. Required for user tokens with more than one organisation.
      schema: { type: string }
    id:
      name: id
      in: path
      required: true
      description: The resource UUID.
      schema: { type: string }
    limit:
      name: limit
      in: query
      required: false
      schema: { type: integer, default: 100, maximum: 500, minimum: 1 }
    offset:
      name: offset
      in: query
      required: false
      schema: { type: integer, default: 0, minimum: 0 }

  responses:
    Error:
      description: Error envelope.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }

  schemas:
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code: { type: integer }
            message: { type: string }
            key: { type: string, description: Stable slug to branch on. }

    Pagination:
      type: object
      properties:
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }

    Position:
      type: object
      description: A document line item.
      properties:
        title: { type: string }
        description: { type: string }
        quantity: { type: number }
        price: { type: number, description: Net unit price. }
        tax_percent: { type: number, description: Percentage, e.g. 20. }
        article_id: { type: string, nullable: true, description: Source catalog article UUID (0/absent for free text). }
        article_variant_id:
          type: string
          nullable: true
          description: >-
            Variant of that article this line was priced from, or absent for a
            plain article line. A variant is a named set of OVERRIDES on the
            article (see `GET /articles/{id}/variants`); picking one takes its
            values instead of the article's own.
        variant_label:
          type: string
          description: >-
            The variant's name at the moment the line was written, SNAPSHOTTED
            like the price. Deleting or renaming the variant later never changes
            what a document says was sold. Resolved server-side - name
            `article_variant_id` and leave this empty to have it filled in.
        supply_type:
          type: string
          enum: ["", service, goods]
          description: >-
            Whether this line is a service or goods. It decides which sentence a
            cross-border document prints: within the EU a service line carries
            the reverse-charge note and a goods line the intra-community supply
            note; outside it, "not taxable at the recipient's place" against an
            export delivery. Empty behaves as `service`.
        fibu_code: { type: string, nullable: true }
        discount_type: { type: string, enum: [none, percent, amount], default: none }
        discount_value: { type: number, description: Percent 0-100 for `percent`, else a EUR amount. }
        commission_eligible: { type: boolean }
        unit:
          type: string
          description: >-
            Short unit printed after the quantity, e.g. "h", "Stk.". **Never
            refused, whatever you send**, and stored as TEXT rather than as a
            catalogue key - which is what keeps an issued document printing
            what it printed on the day it was issued.


            Two ways to send it. A **catalogue key** (`piece`, `hour`, `month`
            - see `GET /units`) is resolved to the printed short form and
            plural **in the issuing organisation's language** and both are
            snapshotted onto the line, so `piece` reads back as `pc.`/`pcs.`
            for an English organisation and `Stk.`/`""` for a German one.
            **Anything else is taken verbatim** as printed text, with
            `unit_plural` from your body when supplied, else looked up by the
            printed short form.


            **So the value you read back is often not the value you sent.**
            That is by design; sending the resolved value back is stable.
            Omitting it is normal - it then falls back to the named article's
            unit and finally to `piece`. There is no length limit and no
            `unit_too_long` refusal any more.
        unit_plural:
          type: string
          description: >-
            Form printed once the quantity leaves 1 ("12 Monate"). Empty means the
            unit does not inflect ("8 h") and `unit` prints unchanged. Snapshotted
            on the position, so renaming or deleting a unit never re-inflects an
            issued document. Resolved server-side - send `unit` and leave this
            empty to have it filled in.
        billing_mode:
          type: string
          enum: [one_time, recurring]
          default: one_time
          description: >-
            Whether the line is a single charge or a fee that repeats. Meaningful
            on QUOTES: a quote can offer one-time and recurring work together, and
            its summary is split per modality rather than printing one combined
            total. Converting such a quote produces an invoice for the one-time
            lines AND a recurring invoice per interval. Defaults to `one_time`, so
            omitting it reproduces the previous behaviour exactly.
        recurring_interval:
          type: string
          enum: [monthly, quarterly, yearly]
          description: >-
            Only meaningful while `billing_mode` is `recurring`; empty otherwise.
            Defaults to `monthly` if a recurring line names none.
        line_net:
          type: number
          readOnly: true
          description: >-
            Line total after its discount. Read-only and authoritative - do NOT
            re-derive it as `quantity * price`, which is wrong for any line
            carrying a discount.
        line_tax:
          type: number
          readOnly: true
          description: VAT for this line, computed on the DISCOUNTED net.
        line_gross:
          type: number
          readOnly: true
          description: line_net + line_tax.

    Recipient:
      type: object
      properties:
        type: { type: string, enum: [business, consumer] }
        company_name: { type: string }
        attention: { type: string }
        salutation: { type: string }
        person_name: { type: string, description: "Full name of a consumer (private person), as ONE field. Replaced first_name/last_name in 0.7.0." }
        address: { type: string }
        zip: { type: string }
        city: { type: string }
        country: { type: string, description: ISO-2 country code. }
        vat_id: { type: string }
        email: { type: string }
        additional: { type: string }

    ReferenceField:
      type: object
      properties:
        label: { type: string }
        value: { type: string }

    DetachedPositions:
      type: array
      readOnly: true
      description: >-
        Lines whose catalog reference was dropped as the document was issued,
        because the article behind it had been retired. Nothing about the line's
        money changes - the position carries its own snapshot of everything that
        prints - but it is reported rather than done silently. Present only when
        it happened.
      items:
        type: object
        properties:
          position: { type: integer, description: 1-based index of the line. }
          title: { type: string }

    Unit:
      type: object
      additionalProperties: true
      description: >-
        One entry of the fixed system unit catalogue. Read-only, and identical
        for every organisation. `key` is what an ARTICLE's `unit` must be; the
        per-language blocks are what a POSITION's `unit` is resolved to and
        snapshotted as.
      properties:
        id: { type: string, description: Same value as `key`. Present so the row has the `id` every entity here has. }
        key:
          type: string
          description: >-
            The catalogue key, lowercase, e.g. `piece`, `hour`, `month`. THIS
            is the value an article's `unit` must be, matched exactly - a
            printed short form such as "Stk." or "h" is not a key and answers
            422 `unit_unknown`.
        de: { $ref: "#/components/schemas/UnitForms" }
        en: { $ref: "#/components/schemas/UnitForms" }

    UnitForms:
      type: object
      additionalProperties: true
      description: The printed forms of a unit in one language.
      properties:
        short:
          type: string
          description: The printed short form, e.g. "Stk." (de) or "pc." (en). This is what a position stores.
        label:
          type: string
          description: The long human name, e.g. "Stück" (de), "Piece" (en).
        plural:
          type: string
          description: >-
            The form printed once the quantity leaves 1, e.g. "pcs.". Empty
            means the unit does not inflect ("8 h") and the short form prints
            unchanged.

    UnitList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/Unit" }

    DocumentTemplate:
      type: object
      additionalProperties: true
      description: >-
        A document template ("Vorlage"). Read-only here, and where the ids for
        the write-only `document_template_id` input come from. Sending that id
        LOADS this template's two terms texts and its days onto a document, which
        then owns its own snapshot - editing or deleting the template never
        changes a document that already exists.


        Replaced `DocumentTerm` (see `GET /document-templates`).
      properties:
        id: { type: string }
        kind:
          type: string
          enum: [quote, invoice]
          description: >-
            Which document type this template belongs to. **`invoice`, not
            `outbound_invoice`** - the kinds differ from the removed
            `/document-terms`, so a value copied from older code is unknown.
            Naming a template of the wrong kind on a document is refused with 404
            `document_template_not_found`.
        label: { type: string, description: What this template is called in the portal. }
        is_default:
          type: boolean
          description: >-
            Whether a new document of this kind starts from this template. At
            most one per organisation and kind - and since the last template of a
            kind cannot be deleted, a default always exists.
        terms_text:
          type: string
          description: >-
            The document's own terms, snapshotted onto it as `terms_text`. The
            portal labels this "Angebotsbedingungen" on a quote and
            "Rechnungsbedingungen" on an invoice.
        payment_terms_text:
          type: string
          description: >-
            The payment terms, snapshotted onto the document as
            `payment_terms_text` and printed AFTER `terms_text`. Labelled
            "Anzahlungsbedingungen" on a quote and "Zahlungsbedingungen" on an
            invoice.
        days:
          type: integer
          description: >-
            Validity (quote) or payment term (invoice) in days. **0 is a real
            value** meaning due immediately. Applying the template moves the
            document's `valid_until` / `due_date` by this many days from its
            issue date. With no template at all the fallback is 30 days for a
            quote and 14 for an invoice.
        footer: { type: string, description: The footer printed on documents started from this template. }
        email_body: { type: string, description: The cover-mail text those documents are sent with. }
        document_ids:
          type: array
          items: { type: string }
          description: >-
            Uuids of the documents a new document of this kind starts with
            attached. Carried at CREATION only - naming a template on an existing
            document loads its texts and days, never its attachments.
        deposit_type:
          type: string
          enum: [none, percent, amount, goods_percent, service_percent]
          description: The down payment a new QUOTE starts with. Meaningless on an invoice template.
        deposit_value: { type: number, description: See `Quote.deposit_value`. }
        bank_mode:
          type: string
          description: >-
            How the bank details print on an INVOICE from this template.
            Meaningless on a quote template - a quote no longer carries bank
            details at all; a down payment invoice is what asks for money now.
        bank_text: { type: string, description: Free-text bank details, used when `bank_mode` says so. }
        bank_account_id:
          type: string
          description: Uuid of the bank account those invoices print. Empty when none is designated.
        can_delete:
          type: boolean
          description: >-
            False for the last template of its kind, which cannot be deleted -
            every document resolution needs one to fall back to.
        created_at: { type: string, format: date-time, nullable: true }

    DocumentTemplateList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/DocumentTemplate" }

    Customer:
      type: object
      additionalProperties: true
      properties:
        payment_mode:
          type: string
          enum: [direct_debit, all, individual]
          description: >-
            How this record is settled. `direct_debit` means WE collect it under
            a SEPA mandate and no way to pay is offered at all; `all` means the
            customer pays by the organisation's standard setting; `individual`
            means they pay by the ways in `payment_methods`. Direct debit is
            deliberately not one of `payment_methods` - it is the level above
            them, so "collect and also offer a transfer" cannot be expressed.
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
          description: >-
            The individual selection, used when `payment_mode` is `individual`.
            Kept when the mode is anything else, so a record returns to these
            ways if a mandate is revoked. Online payment is only ever offered
            when the organisation has a working Stripe configuration.
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this record; null = system-generated (e.g. an automated import)." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        display_name: { type: string, readOnly: true, description: "Der anzuzeigende Name: bei einer Firma company_name, bei einer Privatperson salutation + person_name. Ersetzt das entfallene 'label'." }
        customer_type: { type: string, enum: [business, consumer] }
        company_name: { type: string }
        attention: { type: string }
        salutation: { type: string }
        person_name: { type: string, description: "Full name of a consumer (private person), as ONE field. Replaced first_name/last_name in 0.7.0." }
        homepage: { type: string, description: "Website of this customer, stored without the scheme (e.g. `www.example.at`)." }
        imprint_url: { type: string, description: "Imprint URL. Informational only - never printed on a document." }
        privacy_url: { type: string, description: "Privacy policy URL. Informational only." }
        address: { type: string }
        zip: { type: string }
        city: { type: string }
        country: { type: string, description: ISO-2 country code. }
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            The language documents for this customer go out in - PDF, cover
            mail and public document page alike. A quote or invoice SNAPSHOTS
            it at creation, so moving the customer to another language never
            re-languages a document already issued.


            In practice you will only ever read `DE` or `EN`: a customer with
            no language set is stamped with one when it is saved, derived from
            `country` (DACH -> DE, everything else -> EN) and falling back to
            the organisation's own language when there is no country. `""` is
            the not-yet-stamped state and is not something you can store.


            Sending it explicitly wins over the derivation and is a decision,
            not a hint: changing `country` afterwards does NOT move it.
        vat_id: { type: string, description: UID / VAT number, printed on the document. }
        additional: { type: string, description: Free-form extra address lines. }
        email: { type: string, description: General address. `recipient_email` is what documents are sent to. }
        customer_info: { type: string, readOnly: true, description: The composed address block, as printed. }
        recipient_email: { type: string, description: Where this customer's documents are sent. }
        recipient_email_name: { type: string }
        cc_email: { type: string }
        cc_email_name: { type: string }
        bank_account_holder: { type: string }
        bank_iban:
          type: string
          description: >-
            Kept grouped and printed as stored. Only the CHARACTER SET is
            validated (A-Z, 0-9, spaces) - 422 `iban_invalid_characters`
            otherwise - deliberately not the full IBAN structure, because this
            field also legitimately holds a US account number.
        bank_bic: { type: string }
        document_send_mode:
          type: string
          enum: ["", link, attach]
          description: >-
            How this customer's documents reach them: a link to the public page,
            or the PDF attached. **A customer is the ONE level that may leave
            this empty** ("platform default") - it is a live rule, so an empty
            value defers to the organisation. A quote or invoice always resolves
            it to a concrete value at creation, because a document is the record
            of what was decided for it.
        partner_id: { type: string, nullable: true, description: Commission partner stamped onto this customer's documents. }
        partner_commission_percent:
          type: number
          description: "-1 = inherit the partner profile's own rate; >= 0 = a per-customer override."
        hourly_rate:
          type: number
          nullable: true
          description: Null = inherit the organisation's rate. 0 is a real rate meaning free.
        inherited_rate: { type: number, readOnly: true, description: What hourly_rate would resolve to with no override. }
        payment_method:
          type: string
          readOnly: true
          description: LEGACY. Read `payment_mode` + `payment_methods` instead.
        contacts:
          type: array
          description: >-
            Contact people. Replaced the flat contact_name / contact_email /
            contact_phone trio; phones are E.164, e.g. "+436601791301".
          items:
            type: object
            properties:
              type: { type: string }
              name: { type: string }
              email: { type: string }
              phone: { type: string }
        reverse_charge: { type: boolean }
        balance: { type: number, description: Signed; positive = customer holds credit. }
        risk_level: { type: integer, description: Read-only dunning risk indicator. }
        quote_template_id:
          type: string
          nullable: true
          description: >-
            Document template applied to a QUOTE when this customer is assigned
            to one. A default, not a snapshot: the document takes its own copy,
            so editing the customer never rewrites a document that already
            exists. Must be a template of kind `quote` belonging to the same
            organisation - unknown, foreign and wrong-kind ids all answer 404
            `document_template_not_found`. Null = no customer default; the
            organisation's default template applies instead.


            Replaced `quote_document_term_id`, which was removed.
        quote_valid_days:
          type: integer
          nullable: true
          description: >-
            Validity in days for this customer's quotes, overriding the
            template's own day count. **Null means inherit** - not 0, which is a
            legitimate value meaning "expires the same day". Send null (or an
            empty string) to clear an override.
        quote_terms_override:
          type: boolean
          description: >-
            Whether `quote_terms_text` below replaces the template's terms for
            this customer. **A real switch, not "the text is non-empty"** - on
            with an empty text is the meaningful combination "print no terms at
            all for this customer", which a text-is-empty test cannot express.
        quote_terms_text:
          type: string
          description: >-
            Negotiated terms for this customer's quotes. Applied only when
            `quote_terms_override` is true.
        quote_payment_terms_override:
          type: boolean
          description: The same switch for the second terms text below.
        quote_payment_terms_text:
          type: string
          description: >-
            Negotiated down-payment terms for this customer's quotes. Applied
            only when `quote_payment_terms_override` is true.
        invoice_template_id:
          type: string
          nullable: true
          description: >-
            The same for OUTBOUND INVOICES; must be a template of kind
            `invoice`. It also supplies the payment term, so assigning the
            customer moves `due_date` as well as the texts. Replaced
            `invoice_document_term_id`, which was removed.
        invoice_due_days:
          type: integer
          nullable: true
          description: >-
            Payment term in days for this customer's invoices, overriding the
            template's. **Null means inherit**; 0 is a real value meaning due
            immediately.
        invoice_terms_override:
          type: boolean
          description: Whether `invoice_terms_text` replaces the template's terms for this customer.
        invoice_terms_text:
          type: string
          description: Negotiated terms for this customer's invoices. Applied only when the switch above is true.
        invoice_payment_terms_override:
          type: boolean
          description: Whether `invoice_payment_terms_text` replaces the template's payment terms.
        invoice_payment_terms_text:
          type: string
          description: >-
            Negotiated payment terms for this customer's invoices. Applied only
            when `invoice_payment_terms_override` is true.

    CustomerInput:
      type: object
      additionalProperties: true
      properties:
        payment_mode: { type: string, enum: [direct_debit, all, individual] }
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
        customer_type: { type: string, enum: [business, consumer], default: business }
        company_name: { type: string }
        attention: { type: string }
        salutation: { type: string }
        person_name: { type: string, description: "Full name of a consumer (private person), as ONE field. Replaced first_name/last_name in 0.7.0." }
        homepage: { type: string, description: "Website of this customer, stored without the scheme (e.g. `www.example.at`)." }
        imprint_url: { type: string, description: "Imprint URL. Informational only - never printed on a document." }
        privacy_url: { type: string, description: "Privacy policy URL. Informational only." }
        address: { type: string }
        zip: { type: string }
        city: { type: string }
        country: { type: string, description: ISO-2 country code. }
        language:
          type: string
          enum: [DE, EN]
          description: >-
            The language documents for this customer go out in. Omit it and the
            server derives one from `country` (DACH -> DE, else EN, falling
            back to the organisation's own language when there is no country)
            and stamps it on save; send it and your value wins for good - a
            later change of `country` does NOT move it. Case-insensitive, so
            `en` is accepted.


            Two silent behaviours worth knowing: a body that does not mention
            the key leaves the stored value untouched, and a value that is
            neither DE nor EN (including `""`) is **ignored rather than
            refused** - the record keeps what it had, or takes the derivation
            if it had nothing. So a typo here does not fail, it just does not
            apply.
        vat_id: { type: string }
        additional: { type: string }
        email: { type: string }
        recipient_email: { type: string }
        recipient_email_name: { type: string }
        cc_email: { type: string }
        cc_email_name: { type: string }
        bank_account_holder: { type: string }
        bank_iban:
          type: string
          description: >-
            Character-set validated only (422 `iban_invalid_characters`).
            Writing one here also creates a real bank account for this customer.
        bank_bic: { type: string }
        document_send_mode:
          type: string
          enum: ["", link, attach]
          description: Empty = platform default; only a customer may leave it open.
        partner_id: { type: string, description: Empty clears it. A foreign partner is refused. }
        partner_commission_percent:
          type: number
          nullable: true
          description: Null or empty clears the override back to the partner profile's rate.
        hourly_rate:
          type: number
          nullable: true
          description: Null or empty clears the override back to the organisation's rate. 0 means free.
        name:
          type: string
          writeOnly: true
          description: Legacy alias of `person_name`.
        contacts:
          type: array
          description: Contact people; replaced the flat contact_* trio.
          items:
            type: object
            properties:
              type: { type: string }
              name: { type: string }
              email: { type: string }
              phone: { type: string }
        reverse_charge: { type: boolean }
        quote_template_id:
          type: string
          nullable: true
          description: >-
            Document template applied to a QUOTE when this customer is assigned
            to one. A default, not a snapshot: the document takes its own copy,
            so editing the customer never rewrites a document that already
            exists. Must be a template of kind `quote` belonging to the same
            organisation - unknown, foreign and wrong-kind ids all answer 404
            `document_template_not_found`. Null = no customer default; the
            organisation's default template applies instead.


            Replaced `quote_document_term_id`, which was removed.
        quote_valid_days:
          type: integer
          nullable: true
          description: >-
            Validity in days for this customer's quotes, overriding the
            template's own day count. **Null means inherit** - not 0, which is a
            legitimate value meaning "expires the same day". Send null (or an
            empty string) to clear an override.
        quote_terms_override:
          type: boolean
          description: >-
            Whether `quote_terms_text` below replaces the template's terms for
            this customer. **A real switch, not "the text is non-empty"** - on
            with an empty text is the meaningful combination "print no terms at
            all for this customer", which a text-is-empty test cannot express.
        quote_terms_text:
          type: string
          description: >-
            Negotiated terms for this customer's quotes. Applied only when
            `quote_terms_override` is true.
        quote_payment_terms_override:
          type: boolean
          description: The same switch for the second terms text below.
        quote_payment_terms_text:
          type: string
          description: >-
            Negotiated down-payment terms for this customer's quotes. Applied
            only when `quote_payment_terms_override` is true.
        invoice_template_id:
          type: string
          nullable: true
          description: >-
            The same for OUTBOUND INVOICES; must be a template of kind
            `invoice`. It also supplies the payment term, so assigning the
            customer moves `due_date` as well as the texts. Replaced
            `invoice_document_term_id`, which was removed.
        invoice_due_days:
          type: integer
          nullable: true
          description: >-
            Payment term in days for this customer's invoices, overriding the
            template's. **Null means inherit**; 0 is a real value meaning due
            immediately.
        invoice_terms_override:
          type: boolean
          description: Whether `invoice_terms_text` replaces the template's terms for this customer.
        invoice_terms_text:
          type: string
          description: Negotiated terms for this customer's invoices. Applied only when the switch above is true.
        invoice_payment_terms_override:
          type: boolean
          description: Whether `invoice_payment_terms_text` replaces the template's payment terms.
        invoice_payment_terms_text:
          type: string
          description: >-
            Negotiated payment terms for this customer's invoices. Applied only
            when `invoice_payment_terms_override` is true.

    CustomerList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/Customer" }

    ArticleCategory:
      type: object
      additionalProperties: true
      properties:
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        label: { type: string }
        fibu_code: { type: string }

    ArticleCategoryList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/ArticleCategory" }

    Article:
      type: object
      additionalProperties: true
      properties:
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this record; null = system-generated." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        title: { type: string }
        description: { type: string }
        price: { type: number }
        tax_percent: { type: number }
        unit:
          type: string
          description: >-
            The system catalogue KEY this article's lines start from, e.g.
            `piece`, `hour`, `month` (`GET /units`). An article stores the key
            itself, not a printed form - the printing happens per document, in
            the issuing organisation's language. `""` means "no unit".
        article_group_id: { type: string }
        group_label:
          type: string
          readOnly: true
          description: The category's name, resolved server-side. Empty when uncategorised.
        fibu_code: { type: string }
        commission_eligible: { type: boolean }
        supply_type:
          type: string
          enum: ["", service, goods]
          description: >-
            Service or goods, inherited by lines created from this article. It
            decides which sentence a cross-border document prints - see
            `Position.supply_type`. Empty behaves as `service`.
        billing_mode:
          type: string
          enum: [one_time, recurring]
          description: >-
            The default modality for a quote line created from this article: a
            single charge, or a fee that repeats.
        recurring_interval:
          type: string
          enum: ["", monthly, quarterly, yearly]
          description: Only meaningful while `billing_mode` is `recurring`; empty otherwise.
        variant_count:
          type: integer
          readOnly: true
          description: >-
            How many live variants this article has. 0 means the article is used
            as it stands. Read `GET /articles/{id}/variants` for them.

    ArticleInput:
      type: object
      additionalProperties: true
      description: >-
        NOTE: `?strict=1` does NOT validate this endpoint's body - it declares no
        key list, so an unrecognised key is silently discarded here even with the
        flag on. See the strict-mode section of the introduction.
      properties:
        title: { type: string }
        description: { type: string }
        price: { type: number }
        tax_percent:
          type: number
          description: >-
            Percentage, e.g. 20. Omitted on CREATE, it defaults to the
            organisation's own default rate - which may legitimately be 0 for a
            Kleinunternehmer, so never fold a 0 into a fallback.
        unit:
          type: string
          description: >-
            A system catalogue KEY (`GET /units`), e.g. `piece`, `hour`,
            `month` - matched exactly, lowercase, with no case-folding.
            **An unknown value answers 422 `unit_unknown`**, and a printed
            short form is not a key, so `Stk.`, `h` and `pc.` are all refused.
            Omitted on CREATE it stores `piece`; an explicit `""` stores
            "no unit" and is kept. Unlike a position's `unit`, this one is
            validated.
        article_group_id: { type: string }
        fibu_code: { type: string }
        commission_eligible: { type: boolean }
        supply_type: { type: string, enum: ["", service, goods] }
        billing_mode: { type: string, enum: [one_time, recurring] }
        recurring_interval: { type: string, enum: ["", monthly, quarterly, yearly] }

    ArticleList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/Article" }

    ArticleVariant:
      type: object
      additionalProperties: true
      description: >-
        A named set of OVERRIDES on a catalog article - "Premium", "Klein",
        "Kleinunternehmer". Every override field is genuinely nullable: null
        means the article's own value is inherited, and a value - INCLUDING 0 -
        is a real override. A variant that overrides nothing produces exactly
        what the plain article produces.
      properties:
        id: { type: string }
        article_id: { type: string, readOnly: true, description: The article this variant belongs to. Immutable. }
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this record; null = system-generated." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        label: { type: string, description: What this variant is called. Required, never inherited. }
        sort_order: { type: integer }
        title: { type: string, nullable: true }
        description: { type: string, nullable: true }
        price: { type: number, nullable: true, description: "Net unit price. 0 is a real price; null means inherit." }
        tax_percent: { type: number, nullable: true, description: "Percentage, e.g. 20. 0 is a real rate; null means inherit." }
        unit: { type: string, nullable: true }
        fibu_code: { type: string, nullable: true }
        # Nullability is `nullable: true`, the way every other nullable field in
        # this file expresses it - NOT a `null` member in the enum. Listing null
        # there while `type` says `string` names a value the type forbids, so a
        # strict reader makes exactly the branch that carries the meaning
        # ("clear this override") unreachable. The enum names the real values;
        # the keyword beside it says the value may be absent.
        supply_type: { type: string, nullable: true, enum: [service, goods] }
        billing_mode: { type: string, nullable: true, enum: [one_time, recurring] }
        recurring_interval: { type: string, nullable: true, enum: [monthly, quarterly, yearly] }
        commission_eligible: { type: boolean, nullable: true }

    ArticleVariantInput:
      type: object
      additionalProperties: true
      description: >-
        Send only what this variant should OVERRIDE. An omitted field stays
        inherited; an explicit `null` CLEARS an override back to inherited.
      properties:
        label: { type: string }
        sort_order: { type: integer }
        title: { type: string, nullable: true }
        description: { type: string, nullable: true }
        price: { type: number, nullable: true }
        tax_percent: { type: number, nullable: true }
        unit: { type: string, nullable: true }
        fibu_code: { type: string, nullable: true }
        supply_type: { type: string, nullable: true }
        billing_mode: { type: string, nullable: true }
        recurring_interval: { type: string, nullable: true }
        commission_eligible: { type: boolean, nullable: true }
      required: [label]

    ArticleVariantList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/ArticleVariant" }

    Quote:
      type: object
      additionalProperties: true
      properties:
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        recipient_kind:
          type: string
          enum: [customer, lead, supplier, partner]
          description: >-
            Who this quote is addressed to. `customer` is the normal case and
            covers a freely typed recipient too. `lead` is QUOTE-ONLY - an
            outbound invoice accepts only customer, supplier and partner, since
            an invoice always names a customer.
        recipient_ref_id:
          type: string
          nullable: true
          description: >-
            Uuid of the record named by `recipient_kind`, or null for a
            customer-addressed quote. The printed recipient block is a
            SNAPSHOT taken from that record when it was assigned - editing the
            lead, supplier or partner afterwards never rewrites an existing
            quote.
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this record; null = system-generated." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        public_index: { type: string }
        version: { type: integer }
        status: { type: string, enum: [draft, issued, won, lost, converted] }
        customer_id: { type: string }
        customer_label:
          type: string
          readOnly: true
          description: >-
            The LIVE customer's display name - what a list should show, since it
            follows a later rename. `recipient` is the frozen snapshot the
            document prints and deliberately does not. Falls back to the snapshot
            when the quote has no linked customer.
        partner_id: { type: string, nullable: true, description: Commission partner. }
        project_id: { type: string, nullable: true }
        recipient: { $ref: "#/components/schemas/Recipient" }
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }
        valid_until: { type: string, format: date, nullable: true }
        email_text: { type: string, description: Cover-mail text. Also emitted as `intro_text` (legacy alias). }
        pdf_footer: { type: string, description: Printed footer. Was `footer_comment`, which is no longer emitted. }
        document_ids:
          type: array
          items: { type: string }
          description: UUIDs of the documents attached when this quote is emailed.
        document_send_mode:
          type: string
          enum: [link, attach]
          description: >-
            How the document reaches the customer - a link to the public page, or
            the PDF attached. A quote always carries a concrete value: it is
            resolved at creation from the customer, else the organisation. Only a
            CUSTOMER may leave this open.
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            The language this quote is rendered and mailed in. Stamped at
            creation from **the recipient record's own `language` field**
            (`Customer.language`), NOT recomputed from the address. That
            distinction matters: a customer in Germany whose `language` was set
            to EN produces an EN quote, so predicting this from
            `recipient.country` gets it wrong for exactly those customers.
            Country only enters one step earlier, when the customer itself is
            stamped (DACH -> DE, else EN, else the organisation's language).


            An explicit value sent in the same request always wins over that
            derivation, and it is a SNAPSHOT: assigning a different customer
            later, or renaming the current one, never rewrites a quote that
            already exists.


            `""` on a quote you did not just write means it predates this
            field - resolve it from the linked customer's `language`, else
            `recipient.country`, else the organisation's language.
        reference_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
        reverse_charge: { type: boolean }
        terms_text:
          type: string
          description: >-
            The effective terms text, snapshotted onto the document at creation
            from the selected or default document template. Editing or deleting
            the template never changes an issued document. Labelled
            "Angebotsbedingungen" on a quote.
        payment_terms_text:
          type: string
          description: >-
            The SECOND terms text, snapshotted the same way and printed AFTER
            `terms_text`. On a quote it holds the down-payment terms
            ("Anzahlungsbedingungen"), and **it is printed only when the quote
            actually carries a down payment** - `deposit_type` set AND
            `deposit_value` above zero. A quote at `percent` / `0` shows no
            deposit line in its summary, so printing a paragraph about one would
            describe something invisible.


            Converting a quote does NOT carry this field onto the invoice: on a
            quote it means the deposit terms, on an invoice the payment terms,
            so copying it across would move one meaning into another.
        small_business:
          type: boolean
          description: >-
            The issuer's small-business (Kleinunternehmer) VAT exemption,
            snapshotted at creation - an issued document keeps the status that
            applied when it was issued.
        vat_exemption_note:
          type: string
          nullable: true
          description: >-
            The statutory sentence to print when `small_business` is true; null
            otherwise. **An exempt document must show a single total and this
            note INSTEAD of a net/VAT breakdown** - rendering it like an ordinary
            0 % document is legally wrong. The citation differs per issuer
            country (AT, DE, CH, plus a neutral fallback) and follows the
            organisation's language, so print it as given rather than deriving
            your own.
        deposit_type:
          type: string
          enum: [none, percent, amount, goods_percent, service_percent]
          default: none
          description: >-
            Down payment (Anzahlung). `percent` and `amount` apply to the whole
            ONE-TIME portion of the quote; `goods_percent` and `service_percent`
            apply a percentage to only the goods or only the service lines,
            decided per line by its `supply_type` (a line that states none counts
            as a service). Recurring lines are never part of a down payment, and
            the computed amount is capped by whichever basis applies.
        deposit_value:
          type: number
          description: >-
            A percent 0-100 for `percent`, `goods_percent` and `service_percent`;
            a EUR amount for `amount`.
        deposit_amount:
          type: number
          description: >-
            Resolved down payment, capped to [0, basis] - the basis being the
            one-time gross total, or only its goods or service part for the two
            supply-specific types. It is NOT capped to `gross_total`, which
            includes recurring lines a down payment never covers.
        remaining_amount:
          type: number
          description: >-
            The one-time gross total minus `deposit_amount` - not `gross_total`
            minus it, since recurring lines sit outside the down payment.
        net_total: { type: number }
        tax_total: { type: number }
        gross_total: { type: number }
        converted_outbound_invoice_id:
          type: string
          nullable: true
          readOnly: true
          description: >-
            The invoice this quote was converted into, once it was. **It is
            single-valued**, so it cannot describe a quote billed in stages: a
            down payment and a final invoice are two invoices. Read
            `OutboundInvoice.quote_id` to find all of them.
        converted_recurring_invoice_id:
          type: string
          nullable: true
          readOnly: true
          description: >-
            The recurring template its recurring lines produced, if any. The v1
            convert action never creates one - see `POST /quotes/{id}/convert`.
        accepted_at: { type: string, format: date-time, nullable: true, readOnly: true, description: When the customer accepted it on the public page. }
        has_signature: { type: boolean, readOnly: true, description: Whether that acceptance carries a drawn signature. }
        rejected_at: { type: string, format: date-time, nullable: true, readOnly: true }
        rejection_comment: { type: string, readOnly: true }
        view_count: { type: integer, readOnly: true, description: Opens of the public quote page. }
        download_count: { type: integer, readOnly: true }

    QuoteInput:
      type: object
      additionalProperties: true
      description: >-
        **`document_term_id` was RETIRED.** Sending that key - with any value,
        including null or an empty string - answers 422
        `document_term_id_retired`. Use `document_template_id`. It is refused
        rather than ignored on purpose: ignoring it would leave the document on
        the default template and silently move `valid_until`, so a caller who
        thought they had chosen a preset would get a different date.
      properties:
        recipient_kind:
          type: string
          enum: [customer, lead, supplier, partner]
          description: >-
            Address this quote to something other than a customer. Pass
            `recipient_ref_id` with it: the recipient block is then snapshotted
            server-side from that record and `customer_id` is cleared. A kind
            this document does not accept is REFUSED with 422
            `recipient_kind_invalid` - it is never silently ignored, which would
            leave a document with no recipient at all. `lead` is quote-only.
        recipient_ref_id:
          type: string
          description: >-
            Uuid of the lead, supplier or partner named by `recipient_kind`.
            Unknown and foreign uuids answer identically with 404
            `recipient_not_found`.
        document_template_id:
          type: string
          writeOnly: true
          description: >-
            LOAD a document template ("Vorlage"): its `terms_text`,
            `payment_terms_text` and day count are copied onto the document,
            which then owns its own snapshot. Write-only on purpose - nothing
            records which template was used, because the document's terms are
            editable afterwards and a stored reference would go on naming a
            template whose text no longer applies. Discover the ids with
            `GET /document-templates?kind=quote`.


            It carries texts and days ONLY. A template's default attachments
            (`document_ids`) are applied when a document is CREATED, never by
            naming a template on one that already exists.


            Replaced `document_term_id`, which now answers 422 - see this
            schema's description.
        terms_text:
          type: string
          description: >-
            Type the terms yourself. Applied AFTER `document_template_id`, so
            sending both loads the template and then keeps your wording - which
            is what "pick a template and edit it in one request" has to mean.
        payment_terms_text:
          type: string
          description: >-
            The second terms text - on a quote, the down-payment terms. Same
            precedence as `terms_text`: applied after the template, so it
            survives loading one in the same request. Printed only when the
            quote carries a real down payment (see the output schema).
        customer_id:
          type: string
          description: >-
            Assigning a customer SNAPSHOTS their recipient, reverse-charge flag,
            commission partner, language and document defaults onto the quote -
            unless the same request states them explicitly, which wins. A
            customer from another organisation is refused with 404
            `customer_not_found`.
        recipient:
          allOf: [ { $ref: "#/components/schemas/Recipient" } ]
          description: >-
            Type the recipient instead of naming a customer. Wins over the
            customer's snapshot when both are sent. A `type: consumer` recipient
            has its printed name composed from salutation + person_name, and
            carries no `attention` line.
        recipient_email: { type: string, description: Where this quote is emailed. Wins over the customer's address. }
        recipient_email_name: { type: string }
        partner_id:
          type: string
          description: >-
            Commission partner for this quote. A foreign partner is refused; an
            empty value clears it. Otherwise inherited from the customer.
        project_id: { type: string, description: Empty clears the link. A foreign project is refused. }
        document_ids:
          type: array
          items: { type: string }
          description: >-
            The exact set of attached documents (empty = attach none). Omit to
            keep the organisation's auto-include selection. A document belonging
            to another organisation is refused.
        document_send_mode: { type: string, enum: [link, attach] }
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            Overrides the language derived from the recipient's country (see
            the output schema). Sending `""` clears the override back to
            unresolved - the same state a quote from before this field
            existed is in. A value outside `DE`/`EN` is not refused; it
            silently normalises to `""`, same as omitting the key.
        email_text: { type: string, description: Cover-mail text for this quote. }
        pdf_footer: { type: string, description: Printed footer. Was `footer_comment`. }
        reference_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
        meta_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
          description: Legacy alias of `reference_fields`. Sending both, this one wins.
        reverse_charge: { type: boolean }
        deposit_type:
          type: string
          enum: [none, percent, amount, goods_percent, service_percent]
          description: See `Quote.deposit_type`.
        deposit_value:
          type: number
          description: >-
            A percent 0-100 for `percent`, `goods_percent` and `service_percent`;
            a EUR amount for `amount`.
        valid_until:
          type: string
          format: date
          description: >-
            Omit to let the document template set the validity window - the
            customer's own day count first (when their switch is on), then the
            customer's template, then the organisation's default template, then
            a 30-day fallback. An explicit value here always wins over all of
            them; send an empty string to clear it.
        version: { type: integer, minimum: 1, maximum: 99, description: Forced to 1 while the quote is a draft. }
        status:
          type: string
          enum: [issued, won, lost]
          description: >-
            Outcome tracking only. These three may be set on one another; `draft`
            and `converted` are never settable and a value outside this set is
            ignored rather than refused.
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }

    QuoteList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/Quote" }

    OutboundInvoice:
      type: object
      additionalProperties: true
      properties:
        recipient_kind:
          type: string
          enum: [customer, supplier, partner]
          description: >-
            Who this invoice is addressed to. `customer` is the normal case.
            Note the quote accepts a fourth kind, `lead`; an invoice never does,
            since it always names a customer - use `POST /leads/{id}/complete`
            first. Served since the field existed, documented from 0.11.0.
        recipient_ref_id:
          type: string
          nullable: true
          description: >-
            Uuid of the supplier or partner named by `recipient_kind`, or null
            for a customer-addressed invoice. The printed recipient block is a
            SNAPSHOT taken when it was assigned.
        payment_mode:
          type: string
          enum: [direct_debit, all, individual]
          description: >-
            How this record is settled. `direct_debit` means WE collect it under
            a SEPA mandate and no way to pay is offered at all; `all` means the
            customer pays by the organisation's standard setting; `individual`
            means they pay by the ways in `payment_methods`. Direct debit is
            deliberately not one of `payment_methods` - it is the level above
            them, so "collect and also offer a transfer" cannot be expressed.
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
          description: >-
            The individual selection, used when `payment_mode` is `individual`.
            Kept when the mode is anything else, so a record returns to these
            ways if a mandate is revoked. Online payment is only ever offered
            when the organisation has a working Stripe configuration.
        invoice_scope:
          type: string
          enum: [full, deposit, final]
          readOnly: true
          default: full
          description: >-
            Which PART of its quote this invoice bills. `full` is the whole
            thing and is what every invoice not created as a staged one reports,
            including every invoice that existed before this field did.
            `deposit` is a down payment; `final` is the remainder after one.


            **Do not add a deposit and a final invoice together as if each were
            the whole amount.** They already sum correctly: the final invoice
            lists the full one-time lines and then carries a NEGATIVE deduction
            line per tax rate for what the down payment already covered, so its
            own `gross_total` is the remainder, and deposit gross + final gross
            equals the quote's one-time gross. The deduction is visible in
            `positions` as a line with a negative `price` - do not filter those
            out as if they were an error.


            Set by `POST /quotes/{id}/convert` via its `scope`; it is not a body
            key on this endpoint.
        quote_id:
          type: string
          nullable: true
          readOnly: true
          description: >-
            The quote this invoice was created from, or null. This is the FULL
            trail: one quote may produce several invoices (a down payment and a
            final one), which is exactly what `Quote.converted_outbound_invoice_id`
            cannot express, holding only one. To find every invoice belonging to
            a quote, match on this field rather than reading that one.
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this record; null = system-generated (e.g. a recurring run or a dunning fee invoice)." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        public_index: { type: string }
        status: { type: string, enum: [draft, issued] }
        customer_id: { type: string }
        recipient: { $ref: "#/components/schemas/Recipient" }
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }
        issue_date: { type: string, format: date, nullable: true }
        due_date: { type: string, format: date, nullable: true }
        due_days:
          type: integer
          nullable: true
          readOnly: true
          description: >-
            The payment term in calendar days, DERIVED as `due_date -
            issue_date`. Null while either date is unset - an invoice with no due
            date has no term, and 0 would read as "due today". Writable under the
            same name on the input, where it MOVES the due date.
        customer_label:
          type: string
          readOnly: true
          description: >-
            The LIVE customer's display name - what a list should show, since it
            follows a later rename. `recipient` and `customer_name` are the
            frozen snapshot the document prints and deliberately do not.
        project_id: { type: string, nullable: true }
        document_send_mode:
          type: string
          enum: [link, attach]
          description: >-
            How the document reaches the customer - a link to the public page, or
            the PDF attached. An invoice always carries a concrete value: it is
            resolved at creation from the customer, else the organisation. Only a
            CUSTOMER may leave this open.
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            The language this invoice is rendered and mailed in. Stamped at
            creation from **the recipient record's own `language` field**
            (`Customer.language`), NOT recomputed from the address. That
            distinction matters: a customer in Germany whose `language` was set
            to EN produces an EN invoice, so predicting this from
            `recipient.country` gets it wrong for exactly those customers.
            Country only enters one step earlier, when the customer itself is
            stamped (DACH -> DE, else EN, else the organisation's language).


            An explicit value sent in the same request always wins over that
            derivation, and it is a SNAPSHOT: assigning a different customer
            later, or renaming the current one, never rewrites an invoice
            that already exists. `cancel()`'s credit note carries the
            invoice's own language forward unchanged, never re-resolved.


            `""` on an invoice you did not just write means it predates this
            field - resolve it from the linked customer's `language`, else
            `recipient.country`, else the organisation's language.
        paid: { type: boolean }
        paid_amount: { type: number }
        paid_at:
          type: string
          format: date-time
          nullable: true
          description: >
            Settlement date, derived and never writable. Null until the invoice is
            fully paid, then the date of the payment that settled it (the payment's
            own date, not the recording time). Cleared again if the invoice falls
            back to partially paid.
        remaining_amount:
          type: number
          description: >-
            What is still outstanding on this document. Zero once it is fully
            paid AND zero once it is cancelled: a storno is not a receivable,
            because the credit note that reverses it carries whatever is still
            owed in either direction. Do not re-derive it as gross minus paid -
            that subtraction reports a cancelled invoice as money outstanding.
        cancelled:
          type: boolean
          description: >-
            Whether this invoice was reversed by a cancellation credit note. A
            cancelled invoice keeps its number and its `issued` status - it stays
            part of the record - but owes nothing.
        settlement_status:
          type: string
          enum: [draft, open, partially_paid, paid, cancelled]
          description: >-
            The settlement state as one value, so it never has to be rebuilt out
            of `status` + `cancelled` + `paid`. Prefer it over `paid` whenever
            you need to know whether money is still expected. On a credit note,
            `open` means a refund is still owed to the customer rather than
            money expected from them.
        cancels_outbound_invoice_id:
          type: string
          nullable: true
          description: On a cancellation credit note, the invoice it reverses.
        cancels_outbound_invoice_number:
          type: string
          description: >-
            The human document number behind `cancels_outbound_invoice_id`, so
            the pair can be named without a second request. Empty when unrelated.
        cancelled_by_outbound_invoice_id:
          type: string
          nullable: true
          description: >-
            The reverse direction: on a cancelled invoice, the credit note that
            cancelled it. Null unless this document is cancelled.
        cancelled_by_outbound_invoice_number:
          type: string
          description: The human document number behind `cancelled_by_outbound_invoice_id`.
        dunning_mode:
          type: string
          enum: [auto, include, exclude]
          readOnly: true
          description: >-
            Whether this invoice is eligible for the portal's dunning
            (Mahnwesen) run. `auto` derives it from how the invoice was
            created - an invoice with `source: import` is excluded,
            everything else is included; `include` and `exclude` are
            explicit overrides. **Read-only through this API**: it is set in
            the portal itself, never through this endpoint or the MCP
            connector, so a value here always reflects a decision made
            outside your integration.
        dunning_excluded:
          type: boolean
          readOnly: true
          description: >-
            `dunning_mode` already RESOLVED for this invoice - true when it
            will be left out of the dunning run. Read this one when the
            question is "will this be chased?", and `dunning_mode` when the
            question is "was that decided, or derived?". Under `auto` it is
            derived from `source`, which is why the two only make sense read
            together.
        source:
          type: string
          readOnly: true
          description: >-
            How this invoice came to exist - the field `dunning_mode: auto`
            resolves against. Values seen in practice: `manual` (typed in the
            portal, or created through this API), `recurring` (generated by a
            recurring invoice), `import` (taken over from a previous system,
            and the one value `auto` excludes from dunning), `dunning_fee` (a
            Mahnspesen invoice), `partner_payout`, and `cancellation` (the
            credit note behind a storno). Treat the list as open rather than
            switching on it exhaustively.
        reverse_charge: { type: boolean }
        terms_text:
          type: string
          description: >-
            The effective terms text, snapshotted onto the document at creation
            from the selected or default document template. Editing or deleting
            the template never changes an issued document. Labelled
            "Rechnungsbedingungen" on an invoice, and empty by default - the
            sentence about paying lives in `payment_terms_text`.
        payment_terms_text:
          type: string
          description: >-
            The SECOND terms text, snapshotted the same way and printed AFTER
            `terms_text`. On an invoice these are the payment terms
            ("Zahlungsbedingungen") - the "payable by {due_date}" sentence - so
            an invoice that used to carry its terms in `terms_text` now
            typically carries them here. Render both, in that order.
        small_business:
          type: boolean
          description: >-
            The issuer's small-business (Kleinunternehmer) VAT exemption,
            snapshotted at creation - an issued document keeps the status that
            applied when it was issued.
        vat_exemption_note:
          type: string
          nullable: true
          description: >-
            The statutory sentence to print when `small_business` is true; null
            otherwise. **An exempt document must show a single total and this
            note INSTEAD of a net/VAT breakdown** - rendering it like an ordinary
            0 % document is legally wrong. The citation differs per issuer
            country (AT, DE, CH, plus a neutral fallback) and follows the
            organisation's language, so print it as given rather than deriving
            your own.
        sent_at: { type: string, format: date-time, nullable: true }
        payment_methods_effective:
          type: array
          readOnly: true
          items: { type: string, enum: [online, transfer, cash] }
          description: >-
            The RESOLVED ways to pay THIS document, which is what to render:
            empty under `direct_debit` (we collect, so the customer does
            nothing), the organisation's own defaults under `all`, the document's
            clipped selection under `individual`. Resolved server-side because
            `all` needs the organisation - re-deriving it would be a second
            definition of a money-facing rule. Never render `payment_method`.
        payment_method:
          type: string
          readOnly: true
          description: >-
            LEGACY and frozen - it reads `BANK_TRANSFER` on every row ever
            written, including invoices collected by direct debit. Kept only
            because v1 readers still expect the key. Read `payment_mode` +
            `payment_methods_effective` instead.
        stripe_payment_link_url:
          type: string
          readOnly: true
          description: The online payment link, when the document offers one. Empty otherwise.
        public_link:
          type: string
          readOnly: true
          description: >-
            The customer-facing page for this invoice. Empty before it is issued.
            Its PDF and attachments stop being downloadable 90 days after the
            issue date.
        view_count:
          type: integer
          readOnly: true
          description: >-
            Opens of the public invoice page. **Not an email read receipt**:
            it counts the page being opened, so a PDF handed over any other way
            is invisible here. An absent count is therefore not evidence the
            customer never saw the invoice.
        download_count: { type: integer, readOnly: true, description: Downloads of the invoice PDF from that page. }
        net_total: { type: number }
        tax_total: { type: number }
        gross_total: { type: number }
        partner_id:
          type: string
          nullable: true
          description: >-
            Commission partner. **Commission accrues to THIS field, not to the
            customer's partner** - the customer's is copied here when one is
            assigned, and may then be overridden or cleared for this one invoice.
        send_error:
          type: string
          description: >-
            Present only if issue/send mailing failed. The invoice IS issued - the
            send is best-effort and never fails the issue, which is exactly why
            this has to be read rather than the status code.
        payment_link_error:
          type: string
          description: >-
            Present only if minting the Stripe payment link failed. The invoice IS
            issued, but offers online payment with no link behind it, so the
            customer cannot pay the way the document tells them to.

    OutboundInvoiceInput:
      type: object
      additionalProperties: true
      description: >-
        A draft accepts everything below. An ISSUED invoice accepts the same
        keys EXCEPT the recipient-assignment ones (`customer_id`,
        `recipient_kind`, `recipient_ref_id`), which only apply while it is still
        a draft - an issued document's recipient is a snapshot.


        **`document_term_id` was RETIRED.** Sending that key - with any value,
        including null or an empty string - answers 422
        `document_term_id_retired`. Use `document_template_id`. It is refused
        rather than ignored on purpose: ignoring it would leave the document on
        the default template and silently move `due_date`, so a caller who
        thought they had chosen a preset would get a different date.
      properties:
        payment_mode: { type: string, enum: [direct_debit, all, individual] }
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
        document_send_mode: { type: string, enum: [link, attach] }
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            Overrides the language derived from the recipient's country (see
            the output schema). Sending `""` clears the override back to
            unresolved - the same state an invoice from before this field
            existed is in. A value outside `DE`/`EN` is not refused; it
            silently normalises to `""`, same as omitting the key.
        document_template_id:
          type: string
          writeOnly: true
          description: >-
            LOAD a document template ("Vorlage"): its `terms_text`,
            `payment_terms_text` and day count are copied onto the document,
            which then owns its own snapshot. Write-only on purpose - nothing
            records which template was used, because the document's terms are
            editable afterwards and a stored reference would go on naming a
            template whose text no longer applies. Discover the ids with
            `GET /document-templates?kind=invoice` - note the kind is `invoice`,
            not `outbound_invoice`.


            It carries texts and days ONLY. A template's default attachments
            (`document_ids`) are applied when a document is CREATED, never by
            naming a template on one that already exists.


            Replaced `document_term_id`, which now answers 422 - see this
            schema's description.
        terms_text:
          type: string
          description: >-
            Type the terms yourself. Applied AFTER `document_template_id`, so
            sending both loads the template and then keeps your wording.
        payment_terms_text:
          type: string
          description: >-
            The second terms text - on an invoice, the payment terms, printed
            after `terms_text`. Same precedence: applied after the template, so
            it survives loading one in the same request.
        due_days:
          type: integer
          minimum: 0
          description: >-
            Payment term in days, counted from the issue date; it SETS
            `due_date`. `0` is a real value meaning due immediately, so send it
            as a number rather than leaving it out. An explicit `due_days` beats
            the day count of a `document_template_id` in the same request.


            **This key was accepted and silently ignored until portal v1.4.x.**
            It was read only to switch the terms branch off and then never
            applied, so a request CARRYING it moved nothing - neither the key
            itself nor the days of a template sent beside it, because
            its mere presence disabled that branch. A request without it was
            unaffected, so a preset alone always worked. A client that has been
            sending it as a no-op will now find the due date moving.
            `due_date` itself is NOT an input - it is read-only.
        customer_id:
          type: string
          description: >-
            Draft only. Assigning a customer SNAPSHOTS their recipient, addresses,
            reverse-charge flag, commission partner, payment methods, delivery
            mode, language and document defaults onto the invoice - unless the
            same request states them explicitly, which wins. A customer from
            another organisation is refused with 404.
        recipient_kind:
          type: string
          enum: [customer, supplier, partner]
          description: >-
            Draft only. Bill someone who is not a customer - the path a partner
            commission credit note takes. `supplier` or `partner` snapshots that
            record's address and bank details from `recipient_ref_id`, and
            deliberately CLEARS both `customer_id` and `partner_id`: a credit
            note to a partner must never itself accrue commission.
        recipient_ref_id:
          type: string
          description: The supplier's or partner's UUID; required when `recipient_kind` is one of those.
        recipient:
          allOf: [ { $ref: "#/components/schemas/Recipient" } ]
          description: The printed recipient block, overriding whatever the customer snapshot supplied.
        recipient_email: { type: string, description: Where the invoice is emailed. }
        recipient_email_name: { type: string }
        cc_email: { type: string }
        cc_email_name: { type: string }
        recipient_account_holder:
          type: string
          description: >-
            Where money goes OUT to, on a credit note that will be paid by SEPA
            credit transfer. These three win over the linked customer's own bank
            details.
        recipient_iban: { type: string }
        recipient_bic: { type: string }
        partner_id:
          type: string
          description: >-
            Commission partner for this invoice. Empty CLEARS it, which is how you
            stop an inherited one; a foreign partner is refused. Send it AFTER
            (or with) `customer_id` - assigning a customer seeds it first, then
            this overrides.
        project_id: { type: string, description: Empty clears the link. A foreign project is refused. }
        document_ids:
          type: array
          items: { type: string }
          description: >-
            The exact set of attached documents (empty = attach none). Omit to
            keep the organisation's auto-include selection. A document belonging
            to another organisation is refused.
        service_period: { type: string, description: The Leistungszeitraum printed on the document. }
        tax_label: { type: string, description: Overrides the printed VAT caption. Blank derives "Steuer"/"Tax". }
        email_text: { type: string, description: Cover-mail text for this invoice. }
        pdf_footer: { type: string, description: Printed footer. Was `footer_comment`. }
        pdf_note: { type: string, description: A note printed on the document. Was `invoice_comment`. }
        payment_qr_code: { type: boolean, description: Print the EPC/Girocode next to the bank details. }
        reverse_charge: { type: boolean }
        reference_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
        meta_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
          description: Legacy alias of `reference_fields`; whichever is present is used.
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }

    OutboundInvoiceList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OutboundInvoice" }

    RecurringInvoice:
      type: object
      additionalProperties: true
      properties:
        service_period_mode:
          type: string
          enum: [current, previous, ahead, behind, none]
          description: >-
            Which period the generated invoice states as its Leistungszeitraum.
            `current` and `previous` NAME a calendar period and print its name -
            with a monthly schedule `2027/07`, with a yearly one `2027`.
            `ahead` and `behind` state a SPAN either side of the issue date and
            always print a date range, because a term that does not follow the
            calendar has no name: a yearly schedule issued on 2027-07-01 gives
            `01.07.2027 - 30.06.2028` for `ahead` (prepaid) and
            `01.07.2026 - 30.06.2027` for `behind` (in arrears). A period ends
            the day before the next one starts, so consecutive invoices abut
            exactly. `none` states no period at all.
        payment_mode:
          type: string
          enum: [direct_debit, all, individual]
          description: >-
            How this record is settled. `direct_debit` means WE collect it under
            a SEPA mandate and no way to pay is offered at all; `all` means the
            customer pays by the organisation's standard setting; `individual`
            means they pay by the ways in `payment_methods`. Direct debit is
            deliberately not one of `payment_methods` - it is the level above
            them, so "collect and also offer a transfer" cannot be expressed.
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
          description: >-
            The individual selection, used when `payment_mode` is `individual`.
            Kept when the mode is anything else, so a record returns to these
            ways if a mandate is revoked. Online payment is only ever offered
            when the organisation has a working Stripe configuration.
        id: { type: string }
        organisation_id:
          type: string
          format: uuid
          readOnly: true
          description: >-
            Uuid of the organisation this record belongs to. Useful with a USER
            token, which may span several organisations; an organisation token
            is bound to one and will only ever see its own.
        created_by: { type: string, format: uuid, nullable: true, readOnly: true, description: "Uuid of the user who created this TEMPLATE; null = system-generated. Invoices the automation agent generates from it are always system-created (created_by null), regardless of who created the template." }
        created_by_name: { type: string, readOnly: true, description: "Display name of the creator; empty string when created_by is null." }
        label: { type: string }
        customer_id: { type: string }
        customer_label: { type: string, readOnly: true, description: The LIVE customer's display name. }
        partner_id:
          type: string
          nullable: true
          description: >-
            Commission partner, propagated onto every generated invoice - where
            it, not the customer's, is what commission accrues to.
        project_id: { type: string, nullable: true }
        active: { type: boolean }
        interval: { type: string, enum: [daily, weekly, monthly, yearly] }
        first_run_date:
          type: string
          format: date
          nullable: true
          description: >-
            The schedule's ANCHOR, not a one-off first event: it fixes the day of
            the month (or of the week) that EVERY future run lands on, and moving
            it moves them all. A monthly or yearly schedule anchored on the 31st
            is clamped to the end of shorter months.
        next_run_at: { type: string, format: date-time, nullable: true, description: Derived, read-only. }
        due_days: { type: integer, description: Payment term handed to each generated invoice. }
        payment_method:
          type: string
          enum: [BANK_TRANSFER, DIRECT_DEBIT]
          description: LEGACY. Read `payment_mode` instead.
        document_send_mode:
          type: string
          enum: ["", link, attach]
          description: >-
            How generated invoices reach the customer. A template MAY stay EMPTY,
            unlike a quote or an invoice: it is a rule, not a record, so leaving
            it open lets each generated invoice resolve delivery from the customer
            and then the organisation. A concrete value here overrides both.
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            `""` means "follow the recipient": each generated invoice
            resolves its OWN language at the moment it is created, from its
            customer's country, by the same rule a one-off invoice uses.
            A concrete `DE` or `EN` here overrides that on every invoice this
            template generates, regardless of the customer. Unlike a quote or
            an invoice, a template MAY stay empty on purpose - it is a rule,
            not a record. **Assigning a customer to the template does NOT
            stamp this field**, unlike the customer's terms/day-count
            defaults - only an explicit value here changes it.
        tax_label: { type: string }
        reverse_charge: { type: boolean }
        credit_note: { type: boolean, readOnly: true, description: "Derived - true when the positions net negative." }
        terms_text:
          type: string
          description: >-
            The effective terms text. Copied onto every invoice this template
            generates, alongside the attached documents.
        payment_terms_text:
          type: string
          description: >-
            The SECOND terms text - the payment terms - copied onto every
            generated invoice too, and printed AFTER `terms_text`. Same pair of
            names as on a one-off invoice.
        email_text: { type: string, description: Cover-mail intro copied onto every generated invoice. }
        email_body: { type: string, description: Cover-mail body. }
        pdf_footer: { type: string }
        pdf_note: { type: string }
        reference_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
        document_ids:
          type: array
          readOnly: true
          items: { type: integer }
          description: >-
            Documents attached to every generated invoice. **The one place this
            API exposes INTERNAL INTEGER ids rather than UUIDs** - a wire wart
            that predates the convention and is not settable through this
            contract. Do not build on these values; manage the attachments in the
            portal.
        last_generated_period:
          type: string
          readOnly: true
          description: The period key the last successful run consumed, which is what stops it billing twice.
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }
        last_run_at:
          type: string
          format: date-time
          nullable: true
          readOnly: true
          description: >-
            The last time the portal's automation agent actually ATTEMPTED to run
            this template (generate + send) - never set on a run that was skipped
            because the current period was already generated. Null before the
            first attempt.
        last_run_error:
          type: string
          readOnly: true
          description: >-
            The message from the last attempted run, empty when it succeeded (or
            none has been attempted yet). Cleared by the NEXT successful run, so a
            non-empty value always means "still needs attention" - poll it to
            detect a template the agent could not bill, e.g. because the
            organisation has no SMTP configured. **Read `last_run_severity`
            before calling it a failure**: some messages describe a run that DID
            produce the invoice.
        last_run_severity:
          type: string
          enum: ["", warning, error]
          readOnly: true
          description: >-
            What `last_run_error` means. Empty when there is no message; `error`
            when nothing was billed; `warning` when the invoice WAS generated,
            exported and sent and something after it needs a look. Without this,
            every message reads as a failed run.

    RecurringInvoiceInput:
      type: object
      additionalProperties: true
      description: >-
        **`document_term_id` was RETIRED.** Sending that key - with any value,
        including null or an empty string - answers 422
        `document_term_id_retired`. Use `document_template_id`.


        Note this endpoint does NOT seed a template on create: a recurring
        invoice created through v1 starts with both terms texts empty unless the
        body says otherwise. Quotes and outbound invoices are seeded from the
        organisation's default template; this one is not.
      properties:
        document_template_id:
          type: string
          writeOnly: true
          description: >-
            LOAD a document template ("Vorlage"): its `terms_text`,
            `payment_terms_text` and day count are copied onto the template,
            which then owns its own snapshot. Its days become `due_days` unless
            the same request sends an explicit one. Write-only on purpose -
            nothing records which template was used. Discover the ids with
            `GET /document-templates?kind=invoice` - note the kind is `invoice`,
            not `outbound_invoice`.
        terms_text:
          type: string
          description: >-
            Type the terms yourself; applied AFTER `document_template_id`, so
            sending both loads the template and keeps your wording. Copied onto
            every generated invoice.
        payment_terms_text:
          type: string
          description: >-
            The second terms text - the payment terms - copied onto every
            generated invoice as well, and printed after `terms_text`.
        label: { type: string }
        customer_id:
          type: string
          description: >-
            Assigning a customer applies their document defaults to the
            template. A customer from another organisation is refused with 404.
        partner_id:
          type: string
          description: >-
            Commission partner stamped onto every generated invoice - so a wrong
            value here misdirects commission repeatedly, not once. Empty clears
            it; a foreign partner is refused.
        project_id: { type: string, description: Empty clears the link. A foreign project is refused. }
        active: { type: boolean }
        interval: { type: string, enum: [daily, weekly, monthly, yearly] }
        first_run_date:
          type: string
          format: date
          description: >-
            The schedule ANCHOR - it fixes the day every future run lands on, not
            just the first. Changing it reseeds `next_run_at`.
        service_period_mode:
          type: string
          enum: [current, previous, ahead, behind, none]
          description: Which period each generated invoice states. See the output schema for what each prints.
        due_days:
          type: integer
          description: >-
            Payment term handed to each generated invoice. An explicit value beats
            the day count of a `document_template_id` in the same request.
        document_send_mode:
          type: string
          enum: ["", link, attach]
          description: Empty deliberately allowed here - see the output schema.
        language:
          type: string
          enum: ["", DE, EN]
          description: >-
            Empty deliberately allowed here - see the output schema. `""`
            (the default) lets each generated invoice resolve its own
            language from its own customer.
        payment_mode: { type: string, enum: [direct_debit, all, individual] }
        payment_methods:
          type: array
          items: { type: string, enum: [online, transfer, cash] }
        payment_method: { type: string, enum: [BANK_TRANSFER, DIRECT_DEBIT], description: LEGACY. }
        tax_label: { type: string }
        reverse_charge: { type: boolean }
        email_text: { type: string, description: Cover-mail intro for every generated invoice. }
        email_body: { type: string, description: Cover-mail body. }
        pdf_footer: { type: string }
        pdf_note: { type: string }
        reference_fields:
          type: array
          items: { $ref: "#/components/schemas/ReferenceField" }
        positions:
          type: array
          items: { $ref: "#/components/schemas/Position" }

    RecurringInvoiceList:
      allOf:
        - $ref: "#/components/schemas/Pagination"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/RecurringInvoice" }
